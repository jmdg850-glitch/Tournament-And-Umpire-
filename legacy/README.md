# Legacy PickleLive SPA

The root `src/` tree is the previous PickleLive-extracted single-page app. It is **not** the Tournament product.

Do not connect it to the Tournament App Supabase project (`evuvgxruavnadpbiehgb`).
Do not use NextG (`qfyfomiqxouqftrgganh`) for the new operator/umpire apps.

Tournament product paths:

- `packages/engine`
- `packages/contracts`
- `packages/api`
- `packages/client`
- `apps/operator`
- `apps/umpire`
- `supabase/migrations`
